import { UniqueResultsPipe } from './unique-results.pipe';

describe('UniqueResultsPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueResultsPipe();
    expect(pipe).toBeTruthy();
  });
});
